---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: manyfold-postgres-backup
  namespace: manyfold
spec:
  schedule: "0 3 * * *"
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          securityContext:
            # needed to backup the the /custom/backup/backup location on the host
            runAsUser: 0
            runAsGroup: 0
            fsGroup: 0
          containers:
            - name: pg-backup
              image: ghcr.io/cloudnative-pg/postgresql:18.1-minimal-trixie@sha256:f88715a4dc82c50aa1b4449f4ea0e51fa0a7e2a85d3118ffc1e0708a05f9bc69
              command: ["/bin/sh", "/scripts/backup.sh"]
              env:
                ########################################
                # Change these values when copying cronjob
                ########################################
                - name: APP_NAME
                  value: manyfold
                - name: DATABASE_HOST
                  value: manyfold-postgres-rw
                - name: DATABASE_NAME
                  value: manyfold
                - name: PGUSER
                  valueFrom:
                    secretKeyRef:
                      name: manyfold-postgres
                      key: username
                - name: PGPASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: manyfold-postgres
                      key: password
              volumeMounts:
                - name: backup-volume
                  mountPath: /backups
                - name: script
                  mountPath: /scripts
          volumes:
            - name: backup-volume
              hostPath:
                # this already exists on the host
                path: /custom/backup/backup
                type: Directory
            - name: script
              configMap:
                name: postgres-backup-script
                defaultMode: 0755
